{
  "name": "quests-invite-web",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:quest-og-fallback": "node scripts/generate-quest-share-og-fallback.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
